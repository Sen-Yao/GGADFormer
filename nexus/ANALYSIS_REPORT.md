# Nexus 问题调查报告
## 2026-03-19 11:30 (Asia/Shanghai)

---

## 📊 调查概要

根据用户指导，Nexus 对以下两个问题进行了深入调查：
1. **Elliptic 模型崩溃** - Epoch 22 后 AUC 从 0.7058 跌至 0.2091
2. **Reddit 欠拟合** - AUC 0.5004 vs 论文 0.5782

---

## 🔴 问题 1：Elliptic 模型崩溃

### 现象描述
| Epoch | AUC | 状态 |
|-------|-----|------|
| 1-11 | 0.4208 | 学习阶段 |
| 12-21 | 0.3039 | 性能波动 |
| **22** | **0.7058** | **最佳值** |
| 23-61 | 0.69-0.70 | 稳定但缓慢下降 |
| 62-141 | 0.52-0.60 | 开始崩溃 |
| 142-150 | 0.2091 | 完全崩溃 |

### 数据特征
```
训练集: Counter({0: 4183, 1: 473})
测试集: Counter({0: 33616, 1: 3636})
不平衡比例: 8.85:1
train_rate: 0.1 (10% 用于训练)
总训练样本: 4656
```

### 问题根因分析

**主要原因：batch_size 与数据规模不匹配**

1. **批次数量过少**
   - batch_size = 1024
   - 总训练样本 = 4656
   - 每个 epoch 只有 ~4.5 个批次
   - 梯度估计极度不稳定

2. **重构损失爆炸**
   ```
   WandB 日志显示:
   - batched_total_loss: 93.55
   - rec_loss: 93.45 (占总损失的 99.9%！)
   - bce_loss: 0.025
   - ring_loss: 0.078
   ```
   重构损失完全主导了训练，导致模型崩溃。

3. **学习率过高**
   - peak_lr = 0.0005
   - 对于只有 4.5 个批次的 epoch，学习率过高

### 建议解决方案

| 参数 | 当前值 | 建议值 | 原因 |
|------|--------|--------|------|
| batch_size | 1024 | **4096 或更大** | 增加 batch 数量，稳定梯度 |
| peak_lr | 0.0005 | **0.0001** | 降低学习率防止崩溃 |
| num_epoch | 150 | **50-80** | 早停，避免过拟合 |
| ring_loss_weight | 20 | **1** | 降低 ring loss 权重 |

**额外建议**：
- 添加梯度裁剪 (`torch.nn.utils.clip_grad_norm_`)
- 监控 rec_loss，设置阈值提前停止
- 使用 Early Stopping 保存最佳模型

---

## 🟡 问题 2：Reddit 欠拟合

### 现象描述
- 最终 AUC: 0.5004 (接近随机猜测)
- 论文基准: 0.5782
- 差距: -7.78%

### 数据特征
```
训练集: Counter({0.0: 530, 1.0: 19})
测试集: Counter({0.0: 9026, 1.0: 311})
不平衡比例: 27.9:1
正样本占比: 仅 3.5%！
总训练样本: 549
```

### 问题根因分析

**主要原因：极端类别不平衡 + 样本量过小**

1. **正样本极少**
   - 训练集只有 19 个正样本
   - 这意味着模型几乎无法学习正样本的特征

2. **这是一个公认的难题数据集**
   - 即使论文也只能达到 0.5782（仅比随机好一点）
   - Reddit 数据集的异常检测本身就很困难

3. **batch_size 问题**
   - 当前: 32768
   - 总样本: 549
   - 实际上每个 epoch 只有 1 个批次（无效设置）

### 为什么不是超参数问题？

对比其他成功数据集：
| 数据集 | 正样本数 | AUC vs 论文 |
|--------|---------|-------------|
| Photo | 多 | +3.87% ✅ |
| Amazon | 多 | +2.21% ✅ |
| Reddit | **仅 19** | -7.78% ❌ |

问题不在于超参数，而在于数据本身。

### 建议解决方案

**短期方案（验证复现）：**
1. **多 seed 实验**
   ```bash
   for seed in 0 42 123 456 789; do
       python run.py --dataset=reddit --seed=$seed ...
   done
   ```
   报告均值 ± 标准差

2. **降低 batch_size**
   ```bash
   --batch_size=512  # 或更小
   ```

**长期方案（如果需要改进）：**
- 类别平衡采样
- Focal Loss
- 数据增强
- 针对不平衡数据集的专门方法

---

## 📋 总结

| 问题 | 严重程度 | 根因 | 解决方案 |
|------|---------|------|---------|
| Elliptic 崩溃 | 🔴 高 | batch 太小 + rec_loss 爆炸 | 增大 batch + 降低 lr + 早停 |
| Reddit 欠拟合 | 🟡 中 | 极端不平衡 (正样本仅 19) | 多 seed 实验，接受现实 |

### 优先级建议

1. **优先修复 Elliptic** - 这是可修复的技术问题
2. **Reddit 需要多 seed 验证** - 可能只是 seed 敏感性
3. **如果 Reddit 仍不达标** - 这可能就是数据集的天花板

---

## 📎 相关日志

- Elliptic 日志: `train_elliptic_bs1024_20260319_110356.log`
- Reddit 日志: `train_reddit_20260319_103608.log`
- WandB: https://wandb.ai/HCCS/GGADFormer

---

*报告由 Nexus 自动生成*
*2026-03-19 11:30*